# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/
.venv/
/venv/

# ── Node ─────────────────────────────────────────────────────────────
node_modules/

# ── IDE / Editor ─────────────────────────────────────────────────────
.vscode/
.idea/

# ── Sensitive / secrets ──────────────────────────────────────────────
# Environment files (API keys, tokens, passwords)
.env
.env.*
!.env.example
export*

# API keys & credentials
*.key
*.pem
*.ppk
*.p12
*.pfx
*.jks
*.keystore
privvy*
credentials.json
service-account*.json
*-credentials.yaml
*-credentials.yml
secrets.yaml
secrets.yml
secrets.json
**/secrets/

# OAuth / token caches
token.json
token.pickle
.tokens/
oauth2.json

# SSH keys (if accidentally placed in repo)
id_rsa
id_ed25519
id_ecdsa
*.pub
!docs/**/*.pub

# Config files that may contain real API URLs / keys
cli-config.yaml
config.local.yaml
config.local.yml
*.local.yaml
*.local.yml

# ── Runtime / generated ──────────────────────────────────────────────
logs/
data/
tmp/
temp_vision_images/
images/*
!images/main.png
wandb/
testlogs/
source-data/
hermes-*/*

# ── Project-specific ignores ────────────────────────────────────────
examples/
tests/quick_test_dataset.jsonl
tests/sample_dataset.jsonl
run_datagen_*.sh
browser-use/
agent-browser/
ignored/
mini-swe-agent/

# Skills Hub state
skills/.hub/

# Web UI build output
bullwhip_cli/web_dist/

# Release script temp files
.release_notes.md

# Git worktrees
.worktrees/

# RL benchmarks
environments/benchmarks/evals/

# Nix
.direnv/
result

# Docusaurus generated
website/static/api/skills-index.json
